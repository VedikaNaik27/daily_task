int meetings(vector<int> start,vector<int>stop){
int n=start.size();
sort(start.begin(),start.end);
sort(end.begin(),end.end());
int room=0;
int res=0;
int i=0,j=0;
while(i<n and j<n){
if(start[i]<start[j]){
room++;
res=max(res,room);
i++;
}else{
room--;
j++;
}}
return res;
}
